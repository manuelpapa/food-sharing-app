import React from "react";

import DateTimePicker from "../components/DateTimePicker";

export default {
  title: "DateTimePicker",
  component: DateTimePicker,
};

const Template = () => <DateTimePicker />;

export const DateTimePickerScreen = Template.bind({});
