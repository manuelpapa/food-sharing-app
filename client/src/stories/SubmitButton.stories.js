import React from "react";

import SubmitButton from "../components/SubmitButton";

export default {
  title: "SubmitButton",
  component: SubmitButton,
};

const Template = () => <SubmitButton />;

export const Primary = Template.bind({});
