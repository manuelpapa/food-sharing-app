import React from "react";

import CategoryPicker from "../components/CategoryPicker";

export default {
  title: "CategoryPicker",
  component: CategoryPicker,
};

const Template = () => <CategoryPicker />;

export const AddOfferPicker = Template.bind({});
