import React from "react";

import Form from "../components/Form";

export default {
  title: "Form",
  component: Form,
};

const Template = () => <Form />;

export const AddressForm = Template.bind({});
